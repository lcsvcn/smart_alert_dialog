part of smart_alert_dialog_widget;

class _SmartAlertDialogDesktop extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('smart_alert_dialog_desktop'),
    );
  }
}